#include <stdio.h>

// Punto 2: Implementar una función que imprima "modularizamos!"
char miPrimerFuncion() {
    printf("modularizamos!\n");
}

int main() {
    // Llamar a la función para imprimir el mensaje
    miPrimerFuncion();
    return 0;
}
